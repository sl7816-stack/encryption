# 物理隔離加密堡壘 VAULT V2.8 (LOGIC FIXED)

一個基於 Web Crypto API 開發的純前端、零知識（Zero-Knowledge）文字加密與解密工具。

---

## 🌟 核心特點

1. **軍規級加密標準**：採用 **AES-256-GCM** 認證加密演算法，確保資料的「機密性」與「完整性」（防篡改）。
2. **高強度金鑰衍生**：結合 **PBKDF2-HMAC-SHA256** 演算法（100,000 次疊代演算 + 16 位元組隨機 Salt 鹽值），有效防禦超級電腦暴力破解與彩虹表攻擊。
3. **完全本地化（物理隔離）**：所有加解密運算 100% 在您的瀏覽器端完成，任何密碼、TAG 或明文訊息**絕不傳送至任何伺服器**。
4. **自動 TAG 提取**：密文結構中內建偏移碼（TAG），支援一鍵偵測提取，簡化解密流程。

---

## 🔑 安全最佳實踐 (Security Best Practices)

演算法本身在數學與物理上無法被暴力破解，但**人為因素**是唯一的安全瓶頸。請遵循以下建議以確保極致安全性：

### 1. 根口令（MASTER ROOT）設定建議
- **避免弱密碼**：切勿使用生日、電話、單一英文單字或常見組合（如 `123456`、`password`）。攻擊者會利用字典檔搭配電腦快速猜測。
- **採用「密語組合（Passphrase）」**：建議長度在 **12 字元以上**。例如結合不相關的詞彙或短句：
  - 範例：`Coffee#Taiwan&Hiking2026!`
  - 範例：`日月潭邊吃早餐真快樂888`

### 2. 金鑰傳輸安全（Key Exchange）
- **切勿數位傳輸根口令**：絕對不要透過 LINE、微信、FB Messenger 或 Email 等未加密管道發送「根口令」。
- **推薦約定方式**：
  - 當面口頭約定並記憶。
  - 透過實體紙本遞交。
  - 分拆管道傳輸（例如：一半透過電話口述，另一半透過紙本或不同平台傳送）。

### 3. 端點與裝置安全
- **防止剪貼簿與螢幕側錄**：請確保使用的電腦或手機沒有安裝來源不明的輸入法或木馬軟體。

---

## 🚀 使用說明

### 1. 加密訊息 (Encrypt)
1. 輸入 **🔑 根口令 (MASTER ROOT)**（與接收方事先約定好的秘密口令）。
2. 輸入 **📍 偏移碼 (TAG)**（例如：`8829`，可作為訊息分類或額外驗證標記）。
3. 在右側 **【 明文區 / Plaintext 】** 輸入欲加密的私密訊息。
4. 點擊 **「加密 ➔ 產生密文」** 按鈕。
5. 左側 **【 密文區 / Ciphertext 】** 會生成 Base64 編碼的亂碼，複製該密文傳送給對方即可。

### 2. 解密訊息 (Decrypt)
1. 將收到的亂碼貼入左側 **【 密文區 / Ciphertext 】**。
2. 點擊 **「🔍 偵測左側密文 TAG」** 按鈕，系統會自動提取並填入 **📍 偏移碼 (TAG)**。
3. 輸入正確的 **🔑 根口令 (MASTER ROOT)**。
4. 點擊 **「解密 ⬅ 還原明文」** 按鈕。
5. 解密後的訊息將顯示於右側 **【 明文區 / Plaintext 】**。

---

## 🛠️ 技術規格與密文結構

- **加密演算法**：AES-GCM (256-bit key, 12-byte IV)
- **金鑰衍生函數**：PBKDF2 (HMAC-SHA256, 100,000 iterations, 16-byte random Salt)
- **密文封包格式（Base64 編碼前）**：
  `[1 Byte: TAG長度] + [TAG 內文] + [16 Bytes: Salt] + [12 Bytes: IV] + [AES-GCM 密文與認證標籤]`

---

## 💻 部署與使用方式

本工具為單一 HTML 檔案（`加密V2_8.html`），完全不需要安裝後端服務或數據庫。

1. **直接開啟**：將 HTML 檔案下載至電腦或手機，直接以任何現代瀏覽器（Chrome, Edge, Safari, Firefox）雙擊打開即可使用。
2. **靜態託管**：可部署於 GitHub Pages、Vercel、Netlify 或個人 Server 上供團隊存取。